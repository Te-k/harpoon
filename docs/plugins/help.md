# Help

Show help on a Harpoon plugin, rendering that plugin's documentation
(its class docstring, written in Markdown) to the console.

## Usage

```
harpoon help COMMAND
```

For example, `harpoon help circl` prints the usage documentation for the
`circl` plugin. Running `harpoon help` with no command prints a short
reminder to provide one.
